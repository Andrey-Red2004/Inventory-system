# Inventory-system
I've updated the inventory system. -The main changes were migrating the JavaScript logic to TypeScript. -Buttons that didn't have any interaction are now fully functional. -The visual style is now a very simple style with light mode and dark mode.
